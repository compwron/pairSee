to run:
bin/pairsee /path/to/repo 

to put on path:
ln -s `pwd`/bin/pairsee ~/bin

to run tests:
rspec

Not currently under development

Config file: config/config.yml
contains names, a la
names: Person1 Person2 Person3
